# LEGACY graphics mode: NATIVE mode's font copy enumerates NIO FileSystemProvider
# services, and sshd-sftp's provider (on the test classpath for real-sshd
# interaction tests) cannot instantiate inside the Robolectric sandbox. These
# tests touch SharedPreferences/filesDir only — no graphics needed.
graphicsMode=LEGACY
sqliteMode=LEGACY
