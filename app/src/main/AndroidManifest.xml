<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Sessions switcher: bring a live terminal's own task back to the front. -->
    <uses-permission android:name="android.permission.REORDER_TASKS" />

    <!-- Android Auto Backup is off: secrets are sealed with a per-device
         Keystore key that never leaves the device, so a cloud/D2D restore
         would land hosts and keys whose passwords and key material can no
         longer be decrypted — every host "exists" but none can connect. The
         portable path is Settings → Backup (passphrase-encrypted, works on
         any device) and account-free sync. -->
    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Conch">

        <!-- Disable Sentry's auto-init provider: we initialise manually and
             only when the user opted in. Empty DSN here prevents preInit
             throwing "DSN is required" before App.onCreate runs. -->
        <meta-data
            android:name="io.sentry.auto-init"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".EditHostActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".TerminalActivity"
            android:exported="false"
            android:theme="@style/Theme.Conch.Terminal"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="standard"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|density|fontScale" />

        <!-- Share sheet → upload into a host's shell cwd (iOS drag-and-drop parity). -->
        <activity
            android:name=".ShareToHostActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:label="@string/share_to_host_label">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <activity android:name=".KeysActivity" android:exported="false" />
        <activity android:name=".SnippetsActivity" android:exported="false" />
        <activity android:name=".SettingsActivity" android:exported="false" />
        <activity android:name=".LicensesActivity" android:exported="false" />

        <service
            android:name=".SessionService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".HostsWidgetReceiver"
            android:exported="false"
            android:label="Conch hosts">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/hosts_widget_info" />
        </receiver>

        <!-- SAF: expose SAF-enabled hosts to system file pickers. Managed by
             the platform (MANAGE_DOCUMENTS signature-level permission), roots
             are per-host opt-in (Host.safExpose). -->
        <provider
            android:name=".SftpDocumentsProvider"
            android:authorities="${applicationId}.sftp"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>
    </application>
</manifest>
